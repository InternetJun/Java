package 牛客网;

import java.util.List;

public class NodeAndLinklist {
    /*输入一棵二叉搜索树，将该二叉搜索树转换成一个排序的双向链表。
    要求不能创建任何新的结点，只能调整树中结点指针的指向。*/
    /**{@思路}是对每个节点都加上指向前面数据的指针。怎么遍历出所有的节点呢，并知道他们的上一个指针呢？
     * 有的输入是树的结构，还需要的是怎么实现
     * */
    public List<Node> getMiddleSort(){
        //需要做的是对树进行中序遍历。后面添加指针的思想。
        return null;
    }

}
